import React from 'react';

/**
 * Displays the list of characters affected by surprise
 * (positively or negatively).
 * @return {React.Component}
 */
const SurpriseDetail = () => {
  return (
    <div>Surprise detail</div>
  );
};

export default SurpriseDetail;
